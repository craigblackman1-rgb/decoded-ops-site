# B1 Re-diff — 2026-08-10

Review: content-audit dated 2026-08-08 against staging commit `2cced43`.
Current branch: `lane-b1-rediff` (five commits on since the review baseline).

## Finding status

| # | Finding | Status | Evidence (`file:line`) | Note |
|---|---|---|---|---|
| 1 | `buy-vs-build/page.tsx` still names "Track" | **STILL OPEN** | `app/problems/buy-vs-build/page.tsx:105`, `:147`, `:206` | All three instances remain: "Track is the best platform" (line 105), "where an off-the-shelf platform like Track" (line 147), "I don't sell Track" (line 206). |
| 2 | Homepage rung 1 names "Tracker" | **STILL OPEN** | `app/page.tsx:423` | "Tracker is the best platform I've worked with in this sector" — exactly as the review described. |
| 3 | `how-i-build/page.tsx` names "Tracker" twice | **STILL OPEN** | `app/how-i-build/page.tsx:126`, `:219` | Line 126: "Tracker is the best platform I've worked with in this sector, and I don't sell it or earn anything from recommending it." Line 219: pull-quote "Tracker is the best platform I've worked with in this sector. I don't sell it." Both unchanged. |
| 4 | `retained/page.tsx` — "Discovery Day" link + broken sentence | **STILL OPEN** | `app/retained/page.tsx:206-207`, `:209-210` | Line 206-207: "Start with a Clarity Audit instead. a fractional CTO relationship works best once there's a plan to execute" — lowercase 'a' after a full stop. Line 209-210: "See how Discovery Day works →" — retired product name, links to `/clarity`. |
| 5 | `apps/page.tsx` eyebrow "Four of them" vs plate "Three apps, one platform" | **STILL OPEN** | `app/apps/page.tsx:38`, `:90` | Eyebrow says "Four of them" (line 38). Plate title says "Three apps, one platform" (line 90). The page now lists four app entries (Data App, Artwork Manager, Commerce, CRM) so the eyebrow is correct and the plate title is stale. Either the eyebrow should read "Three" or the plate should read "Four". |
| 6 | `ProblemPage.tsx` and `SectorPage.tsx` are dead code | **STILL OPEN** | `components/ProblemPage.tsx`, `components/SectorPage.tsx` | Repo-wide grep confirms zero imports of either file. They are superseded by `ProblemPageDS.tsx` (imported by 7 files) and `SectorPageDS.tsx` (imported by 9 files). Stale comment at `components/ProblemPageDS.tsx:36`: "ProblemPage.tsx still serves /problems/wrong-erp-software" — that page no longer imports ProblemPage.tsx; it uses the DS version. |
| 7 | Em dash count in live code | **STILL OPEN** | 546 lines across 63 live files | Review reported 29 total, 21 in live code. Current count: 546 lines with em dashes in live code (63 files) + 16 lines in dead code (ProblemPage.tsx: 10, SectorPage.tsx: 6). Total: 562 lines. Top files: `hanicks-proposal.ts:75`, `cobra-workwear-proposal.ts:73`, `tacklebag-proposal-v2.ts:65`, `scotshirts-proposal.ts:57`, `tacklebag-proposal.ts:56`, `cwear-proposal.ts:47`, `about/page.tsx:26`, `Header.tsx:11`. Counts are **lines containing an em dash**, not individual em dash instances — a line may contain many. |
| 8 | "not X, but Y" parallelism at 4 cited lines | **3 STILL OPEN, 1 ALREADY FIXED** | see below | `case-studies/hanicks:74`: "not decorated goods, but the same underlying problem" — **still present**. `how-i-build:42`: no "not…but" pattern on this line or anywhere in the file (grep confirms zero matches) — **already fixed** or never present. `problems/bottleneck-growth:72` (review said :69, but the prose is on :72): "not because you want it to, but because nobody else knows the process" — **still present**. `problems/disaster-recovery:73`: "Not because they don't care, but because DR feels like something for enterprise IT teams" — **still present**. |
| 9 | "unlock" at location page | **STILL OPEN** | `app/locations/tech-audit/[location]/page.tsx:63` | "Identify the three to five changes that would recover the most cost or unlock the most revenue" — the word "unlock" remains. |
| 10 | Sector page secondary CTAs | **STILL OPEN** | see below | Four sector pages point at product pages, five at `/clarity` — exactly as the review described. **Workwear** (`:50` → `/apps/data-app`), **Teamwear/Clubwear** (`:50` → `/apps/data-app`), **Schoolwear** (`:50` → `/apps/data-app`), **Promotional Merchandise** (`:50` → `/apps/artwork-manager`). **Garment Decoration** (`:111` → `/clarity`), **Print & Promotional** (`:106` → `/clarity`), **Signs & Graphics** (`:85` → `/clarity`), **Awards & Engraving** (`:85` → `/clarity`), **Labels & Packaging** (`:85` → `/clarity`). |
| 11 | Sitemap hardcoded slug arrays vs disk | **STILL OPEN** | `app/sitemap.ts:60-93` | **Problem slugs missing from sitemap** (on disk, not in array): `bottleneck-growth`, `ops-in-owners-head`, `seasonal-peaks`, `spreadsheet-addiction`. **In sitemap but not on disk**: none. **Sector slugs**: all 9 match. **Tool slugs**: all 6 match. |
| 12 | `public/llms.txt` — prices and product names | **STILL OPEN** | `public/llms.txt` | **Price mismatches vs current site**: (1) Clarity "From £2,500" → site says "From £1,500". (2) Deliver "From £1,050/month" → site says £1,200–£1,500/mo (under £1m), £795/mo (small biz). (3) Retained "6-month minimum" → site says 12-month minimum for Retained Transformation, 3-month for small biz Retained. (4) Small business "Clarity Check £395" → site says £595. (5) Small business "Monthly from £395" → site lowest is £360/mo (Retained entry). **Stale product name**: "Discovery Day" used in prose (retired, now Clarity Audit). **Misleading label**: "Pricing Questionnaire" links to `/pricing` which is the pricing page, not a questionnaire. |
| 13 | Duplicate metadata in `/pricing` and `/contact` | **PARTIALLY FIXED** | see below | **`/pricing`**: No `layout.tsx` — only `page.tsx` defines metadata. **Already fixed** (or was never a duplicate). **`/contact`**: Both `page.tsx` (`app/contact/page.tsx:5-20`) and `layout.tsx` (`app/contact/layout.tsx:3-7`) define `metadata`. Page title: "Book a Free Discovery Call \| Decoded Ops". Layout title: "Contact Decoded Ops". Per Next.js merging rules, `page.tsx` wins for `title` (the page's metadata overrides the layout's). The duplicate is redundant but not a visible bug. **Still open** as code hygiene. |
| 14 | `<h1>` on `/pricing` and `/contact` | **ALREADY FIXED** | `app/pricing/page.tsx:43`, `app/contact/ContactClient.tsx:75` | `/pricing`: `<h1>Plain pricing. No hidden day rate.</h1>` (line 43). `/contact`: `<h1 className="h1">Start with a free <em>60-minute call.</em></h1>` (line 75). Both pages have `<h1>` elements. |

---

## What is genuinely still open

1. **"Track"/"Tracker" references** (findings 1–3) — 6 instances across 3 files: `buy-vs-build/page.tsx` (105, 147, 206), `page.tsx` (423), `how-i-build/page.tsx` (126, 219). All should be removed entirely (the brand no longer names the platform).

2. **"Discovery Day" link** (finding 4) — `retained/page.tsx:209-210` links to `/clarity` with the label "See how Discovery Day works". The product was renamed. Also the broken sentence at lines 206-207 (lowercase after full stop).

3. **Apps page eyebrow/plate mismatch** (finding 5) — eyebrow says "Four of them", plate says "Three apps, one platform". One is wrong.

4. **Dead code: ProblemPage.tsx + SectorPage.tsx** (finding 6) — zero imports, superseded by DS variants. Stale comment at `ProblemPageDS.tsx:36` claiming ProblemPage.tsx is still used. Both files and the stale comment should be cleaned up.

5. **Em dash proliferation** (finding 7) — 546 lines (across 63 live files) contain em dashes, vs 21 reported. The count has grown, not shrunk.

6. **"not X, but Y" parallelism** (finding 8) — 3 of 4 remain. `hanicks:74`, `bottleneck-growth:72`, `disaster-recovery:73`.

7. **"unlock" at location page** (finding 9) — `tech-audit/[location]/page.tsx:63`.

8. **Sector secondary CTAs** (finding 10) — 4 sector pages point at product pages instead of `/clarity`. The review flagged this as a routing inconsistency.

9. **Sitemap missing 4 problem slugs** (finding 11) — `bottleneck-growth`, `ops-in-owners-head`, `seasonal-peaks`, `spreadsheet-addiction`.

10. **`llms.txt` is stale** (finding 12) — five price discrepancies, one retired product name ("Discovery Day"), one misleading label ("Pricing Questionnaire").

11. **Duplicate metadata in `/contact`** (finding 13) — both `page.tsx` and `layout.tsx` export `metadata`. Redundant but non-breaking.
