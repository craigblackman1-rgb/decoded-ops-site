# Lane brief: WO-INF-061 build · D17 artwork, batch "locations"

Worktree: this directory (off origin/staging, which has the live D17 system: `app/d17-global.css`, `components/D17Motion.tsx`,
and the sector pages as the reference implementation, e.g. `app/sectors/workwear/page.tsx`). Commit here only. Never push.

## Spec (approved by Craig 2026-09-24, blanket go)
Mockup sources: `.context/lane-briefs/d17-locations/` (page snippets *.html, `d17-art.css`, `d17-locations-legal.css`) and mockup renders `*-1440.png`
= the Design Parity target. Image paths in the sources already point at committed files under `/images/d17/locations/`; don't add/move images.

## How to port (the proven sector pattern)
- Inject each art piece as raw HTML with `dangerouslySetInnerHTML` like app/sectors/*/page.tsx, class names unchanged.
- CSS: injected markup uses PLAIN class names, so its CSS must be GLOBAL. Create `app/d17-locations.css` with the full contents of `d17-locations-legal.css`
  and import globally in each page: `import '@/app/d17-global.css'; import '@/app/d17-locations.css';`. NEVER a CSS module for injected markup.
  Prefix generic selectors that clash with app/globals.css (.card .wrap .btn .lede .eyebrow .kicker .grid, bare elements) with `.d17 `.
- Render `<D17Motion />` on each page as the sector pages do.
- Keep each live page's copy/sections and any interactive calculator/form UI and logic exactly as live; only add/replace art slots,
  plus the copy fixes below. Craig's photo must not appear. At 390px nothing may overflow horizontally.
- DO-ART-1002 coverage map must be generated per town at build time: port the coordinate table and SVG layout from gen1002.py/genmap.py into a small TypeScript module (e.g. lib/coverage-map.ts exporting a function that returns the SVG string for a town slug) and inject it in the [location] pages, highlighting that town and placing its pin exactly as the Guildford/Brighton examples show. Replace {{town}}/{{county}}/{{travel}} in the templates with the page's existing location data fields.
- Legal pages: art sits above the text only; don't restyle legal text beyond the mockup's spacing fix.

## Pages
app/locations/fractional-cto/page.tsx, app/locations/fractional-cto/[location]/page.tsx, app/locations/tech-audit/page.tsx, app/locations/tech-audit/[location]/page.tsx, app/privacy/page.tsx, app/cookies/page.tsx. Sources: fractional-cto.main.html, tech-audit.main.html (town TEMPLATES using {{town}}, {{county}}, {{travel}} placeholders), privacy.main.html, cookies.main.html; _1002-brighton.html and _1002-guildford.html are two rendered examples of the per-town coverage map DO-ART-1002, generated by gen1002.py / genmap.py (town coordinates live in those scripts).

## Copy fixes in the same pass
CR-WEB-054: remove third-party company names in the local-fact panels (e.g. 'Bullfrog Productions', 'American Express'): reword to the industry/landmark without a company name; replace em dashes in local paragraphs; tech audit price must read 'From £1,500' everywhere including meta descriptions (remove any £395); the trust line 'On-site across Sussex and Surrey' must be location-aware: keep it for towns in Sussex/Surrey, and for others use 'On-site by arrangement, remote across the UK'.

FORBIDDEN: dev servers, browsers, npm run build/next build, any DB or .env access, pushing, touching pages outside this batch.
DONE: `npx tsc --noEmit` passes; `git grep -n "craig-blackman"` in the batch's dirs is empty; one commit
"feat(web): D17 artwork on locations pages (WO-INF-061)". Report per page: slots replaced, anything not ported and why.
