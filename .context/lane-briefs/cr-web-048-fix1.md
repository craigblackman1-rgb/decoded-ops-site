# Fix pass 1: CR-WEB-048 (same worktree, same rules as cr-web-048.md: copy only, no dev server/build/DB/push)

Spec is still .context/lane-briefs/CASE_STUDIES_COPY_2026-09-23.md. Verbatim copy only.

1. app/case-studies/page.tsx cards: set card 01 `name` to "Hanicks" and card 02 `name` to "The diagnostic came before the decision." (these render as the card h3). Card labels must be exactly "Heating spares & eCommerce · Live engagement" and "Branded apparel & decoration · In Deliver". Card `desc`: 01 = "Hanicks sells heating spares." is NOT enough; use the first sentence of the 01 Intro verbatim ("Hanicks sells heating spares. It isn't decorated goods, but it's the same problem underneath: a big catalogue arriving from lots of suppliers in different formats, which has to be clean and current on every channel it sells through."); 02 = the 02 Intro verbatim. Remove the invented "Heating spares, big catalogue..." desc. Card figures exactly as the spec's "Figures on card" lines.
2. Delete the source comment `{/* TackleBag */}` in app/case-studies/page.tsx (02 must never name the client, even in source).
3. case-study-02 DO-ART-206 plate: After lines must be exactly the four spec After bullets, including "27,778 supplier products (236,056 sizes and colours) in one place" and "The same website platform, with the data app doing the work it was never built for". Remove "Same platform + Data App alongside it". If a line is too long for the SVG, wrap it onto two <text>/<tspan> lines; do not shorten the words.
4. case-study-02 figures block last stat: "20 to 40 hours a week of admin time, projected at discovery (being measured now it's live)".
5. case-study-01 figures: the stock-take stat must read "2,872 new products and 28 new suppliers found in a stock-take reconciliation" (split value/label however the existing tile markup does, but keep both numbers).
6. case-study-01 DO-ART-205 plate: replace the AFTER line "Same platform, kept" with "Data app (now the ERP)". Replace the paraphrased lede under the plate eyebrow with the spec's small print "Catalogue figures from the live system, September 2026."
Then: npx tsc --noEmit; git grep -nE "Khaos|164,752|127,135|11,064|9 supplier feeds|TackleBag|Tackle Bag|Same platform" -- app/case-studies must be empty. One commit "fix(case-studies): card titles, plate lines, full figures (CR-WEB-048)". Report each item done with file:line.
