# Lane B8 + B10 - Sector CTAs and SEO hygiene report

**Date:** 10 August 2026
**Branch:** `lane-b8b10-cta-seo`, off `origin/staging`

---

## Task 1 - Sector secondary CTAs before and after

### Before

| Sector | Secondary CTA label | href |
|---|---|---|
| garment-decoration | See how the audit works | /clarity |
| print-promotional | See how the audit works | /clarity |
| signs-graphics | See how the audit works | /clarity |
| labels-packaging | See how the audit works | /clarity |
| awards-engraving | See how the audit works | /clarity |
| **workwear** | **See the Data App** | **/apps/data-app** |
| **teamwear-clubwear** | **See the Data App** | **/apps/data-app** |
| **schoolwear** | **See the Data App** | **/apps/data-app** |
| **promotional-merchandise** | **See the Artwork Manager** | **/apps/artwork-manager** |

### After

All nine now use `{ label: 'See how the audit works', href: '/clarity' }`.

The SectorPageDS component's CTA strip already hardcodes "See how a Clarity Audit works" -> `/clarity` (line 173), so the bottom-of-page CTA was already correct. Only the hero secondary CTA needed changing.

---

## Task 2 - Sector page closing sections

### Pages that already had closing structure (no changes)

- `garment-decoration` - "Common challenges" (5 cards) + "How I help" (7 cards)
- `print-promotional` - "Common challenges" (6 cards) + "How I help" (6 cards)
- `signs-graphics` - "Common challenges" (4 cards) + "How I help" (6 cards)
- `labels-packaging` - "Common challenges" (4 cards) + "How I help" (6 cards)
- `awards-engraving` - "Common challenges" (4 cards) + "How I help" (6 cards)

### Pages that received closing sections

- **`workwear`** - added "Common challenges" (5 cards: supplier feed chaos, decoration-and-stock, blank stock dependency, eCommerce disconnected from production, replenishment that waits on you) + "How I help" (6 cards: map workflow, catalogue re-keying, blank stock audit, personalisation flow, eCommerce integration, quantify findings)
- **`teamwear-clubwear`** - added "Common challenges" (5 cards: squad personalisation, deadline pressure, kit range churn, sponsor logo management, proof bottleneck) + "How I help" (6 cards)
- **`schoolwear`** - added "Common challenges" (4 cards: supplier feeds before peak, sizing at scale, badge management, parent portal gap) + "How I help" (6 cards)
- **`promotional-merchandise`** - added "Common challenges" (5 cards: multi-supplier catalogue, artwork loop, margin erosion, quote-to-order costs, eCommerce disconnected from supply) + "How I help" (6 cards)

The leading paragraph in each challenges section includes the sector name (e.g. "In workwear, the same operational problems..."), and every card body is written in sector-specific language rather than boilerplate.

---

## Task 3 - Sitemap slug derivation

### Method

Replaced three hardcoded arrays with a `deriveSlugs(subdir)` function that reads directory names from the filesystem in `app/problems/`, `app/sectors/`, and `app/tools/`, filtering out:
- Underscore-prefixed dirs
- Route groups (parenthesised dirs)
- Dynamic segments (`[...slug]`, etc.)
- Directories without a `page.tsx`

### Sectors and tools: no change

All 9 sector slugs and 6 tool slugs matched the hardcoded list exactly. No drift.

### Problems: 4 added

Old hardcoded list (14 slugs): `cant-scale-operations`, `slow-processes`, `erp-implementation-failure`, `ecommerce-not-connected`, `systems-dont-talk`, `wrong-erp-software`, `disaster-recovery`, `no-ops-owner`, `manual-workarounds`, `ai-paralysis`, `buy-vs-build`, `data-scattered`, `inventory-blind`, `legacy-system`

Filesystem list (18 slugs):

| Added | Removed |
|---|---|
| `bottleneck-growth` | (none) |
| `ops-in-owners-head` | |
| `seasonal-peaks` | |
| `spreadsheet-addiction` | |

These four problem pages existed on disk with `page.tsx` files but were excluded from the sitemap by the stale hardcoded array. Now they are automatically included.

---

## Task 4 - `public/llms.txt`

### What was wrong

- **Clarity Audit `From £2,500`** - wrong; it is `From £1,500`
- **Deliver `From £1,050/month`** - price stated; removed
- **Transform `From £1,500/month`** - price stated; removed
- **Retained `From £1,440/month`** - price stated; removed
- **Small Business `Clarity Check £395. Monthly from £395`** - prices stated; removed
- **"Services" section** - presented software as a product line; rewritten as "Consultancy" section with no prices beyond the Clarity Audit anchor
- **"Pricing Questionnaire"** link under Tools - removed (was a duplicate link to /pricing mislabeled as a tool)
- **No mention of software** - added "Software as evidence of capability" section describing builds as serving the audit's findings

### What it now says

- Clarity Audit from £1,500 (only price stated)
- Three other consultancy lines described without prices
- Pricing page linked separately
- Software section positioned as capability evidence
- Small Business section kept price-free
- No mention of Discovery Day or App Scoping Session (both retired)

---

## Task 5 - Metadata and H1s

### /pricing

- **No duplication found.** `app/pricing/layout.tsx` does not exist. Only `app/pricing/page.tsx` defines metadata. No action needed.
- **H1 verified present:** `<h1>Plain pricing. No hidden day rate.</h1>` at `page.tsx:43`. No action needed.

### /contact

- **Duplication found and fixed.** `app/contact/layout.tsx` declared separate metadata (title: "Contact Decoded Ops") while `app/contact/page.tsx` had the more complete export (title: "Book a Free Discovery Call | Decoded Ops", with OG + Twitter). Removed the `metadata` export and `import` from `layout.tsx`. The page-level metadata is the sole source.
- **H1 verified present:** `<h1 className="h1">Start with a free <em>60-minute call.</em></h1>` at `ContactClient.tsx:75`. No action needed.

---

## Verification results

- `npx tsc --noEmit`: cannot run (no `node_modules` in this worktree - expected per CLAUDE.md)
- Em dash grep on changed files: **zero matches** in my 7 changed files
- `app/clients/` changes: **zero** (confirmed via `git diff --stat -- app/clients/`)
- `git diff --stat`: 7 files, 232 insertions, 39 deletions

---

## Decisions not made

- **Did not install node_modules or run `tsc`.** The CLI restart policy for unsupervised pushes (recorded in the design-system workorder) means I should not install packages unattended. The changes are structurally identical to existing patterns in the codebase (same card layouts, same component wrappers, same prop shapes).

- **Did not remove the product-page secondary CTAs from the CTA strips in the older pages.** The 5 older pages already point at `/clarity` in both hero and strip. The 4 newer pages' SectorPageDS component hardcodes the correct `/clarity` in its CTA strip. Only the `heroSecondaryCta` prop needed changing.

- **Did not refactor SectorPageDS to include closing sections.** The component serves the 4 DS-ported pages well; the closing sections are written inline per page to keep them sector-specific (per the task brief). A future refactor could extract the closing pattern into a shared component with a data prop, but that was out of scope.
