# Lane B7 + B9 — completion report

**Date:** 10 August 2026  
**Branch:** `lane-b7b9-fixes-voice` (off `origin/staging`)  
**Files changed:** 54 (146 insertions, 529 deletions)

---

## Task 1 — Factual fixes

### 1a. `app/retained/page.tsx` (link text + broken sentence)

**Line 206–210:** Two fixes in the Retained "it doesn't fit when" panel:

1. **Link text:** "See how Discovery Day works" → "See how a Clarity Audit works".  
   *Rationale:* Discovery Day is a retired product name; Clarity Audit is current.

2. **Broken sentence:** Lowercase "a" after a full stop corrected to "A":  
   *before:* `…instead. a fractional CTO relationship works…`  
   *after:* `…instead. A fractional CTO relationship works…`

The link `href` already pointed to `/clarity`, so this was purely a label change.

### 1b. `app/apps/page.tsx` (eyebrow vs plate count mismatch)

**The conflict:** Section 2's eyebrow read "Four of them" (above a 4-item log-list: Data App, Artwork Manager, Commerce, CRM). Section 2a's h2 and Plate title both read "Three apps, one platform" (in an SVG diagram showing three app nodes around a central "Your platform" node).

**Decision:** Changed the eyebrow on line 38 from `Four of them` to `What I build`.

**Why this, not the plate:** The task instructed to prefer correcting the plate title. However, the SVG plate (DO-ART-104) genuinely only accommodates three app nodes — Data App, Artwork Manager, and Commerce — arranged around the central "Your platform" hub. Adding a fourth node would require redesigning the SVG geometry. The CRM is listed in the log-list above but is not part of the "systems that plug into your platform" architecture diagram, which is a fair conceptual split.

"Four apps, one platform" as a plate title would have been visually misleading since the diagram only renders three. Changing the eyebrow to "What I build" avoids the count contradiction without asserting an incorrect number in either direction.

---

## Task 2 — Delete dead code

### Verification

**`components/ProblemPage.tsx`** — Confirmed dead. Grepped the entire repo for imports:
- Zero runtime imports found anywhere in `app/`, `components/`, or `lib/`.
- The only references were in `components/ProblemPageDS.tsx` comments (now updated).
- The page `/problems/wrong-erp-software/page.tsx` (which the old comment claimed imported it) actually uses its own inline JSX and imports neither `ProblemPage` nor `ProblemPageDS`.

**`components/SectorPage.tsx`** — Confirmed dead. Grepped the entire repo for imports:
- Zero runtime imports found.
- All 9 sector pages (`app/sectors/*/page.tsx`) import `SectorPageDS`, not `SectorPage`.

### Actions

- **Deleted:** `components/ProblemPage.tsx` (202 lines)
- **Deleted:** `components/SectorPage.tsx` (181 lines)
- **Fixed stale comment** at `components/ProblemPageDS.tsx:35–37` — the old comment claimed `ProblemPage.tsx` still served `/problems/wrong-erp-software`. Updated to note the old component has been removed and that page now uses its own inline JSX.

---

## Task 3 — Voice sweep

### Em dashes

**Count before:** 136 em dashes across ~40 files outside `app/clients/`.

**Count after:** 0.

**What was changed:** Every em dash in `app/` and `components/` (excluding `app/clients/`) was replaced with context-appropriate punctuation:

| Context | Replacement | Approx. count |
|---|---|---|
| Code comments (`//`, `{/* */}`, `* ...`) | Comma | ~61 |
| SVG `<text>` labels in ALL CAPS | Colon (`:`) | ~6 |
| Running prose, JSX text, Rail notes, aria-labels, SVG sentence-case text | Comma | ~64 |
| Metadata strings (page titles, email subjects) | Colon (`:`) | ~3 |
| Bullet-like marker (`— Craig Blackman`) | Comma | 1 |

**Notable files:** `components/Header.tsx` (11 dashes), `components/schematics/DiscoveryDaySchematic.tsx` (8), `components/schematics/sectors/PrintPromotionalSchematic.tsx` (6), `components/schematics/problems/CantScaleSchematic.tsx` (5), plus ~35 others.

### "Not X, but Y" parallelism

Five instances fixed (one extra found during sweep):

| File | Line | Before → After |
|---|---|---|
| `app/case-studies/hanicks/page.tsx` | 74 | "not decorated goods, but the same underlying problem" → "rather than decorated goods. The underlying problem is the same" |
| `app/how-i-build/page.tsx` | 45 | "Not because the bottom rung is always right, but because you should" → "The bottom rung isn't always right. You should" |
| `app/problems/bottleneck-growth/page.tsx` | 72 | "not because you want it to, but because nobody else knows" → "You don't want it to. Nobody else knows" |
| `app/problems/disaster-recovery/page.tsx` | 73 | "Not because they don't care, but because DR feels" → "They care. DR just feels" |
| `app/problems/ecommerce-not-connected/page.tsx` | 54 | "not because the integration itself is broken" → "The integration itself is rarely broken" |

### Banned word: "unlock"

**`app/locations/tech-audit/[location]/page.tsx:63`:**  
"unlock the most revenue" → "release the most revenue"

---

## Verification results

| Check | Result |
|---|---|
| No files under `app/clients/` modified | ✅ PASS — `git status --short` confirms |
| Zero em dashes outside `app/clients/` | ✅ PASS — PowerShell Unicode grep confirms |
| `npx tsc --noEmit` | ❌ Cannot run — no `node_modules` in this worktree (expected per CLAUDE.md: "A fresh checkout cannot run tsc or next build until dependencies are installed") |
| `npm run lint` | ❌ ESLint not installed — same reason |

---

## Things deliberately NOT changed (and why)

1. **"pain point" in `components/schematics/sectors/PrintPromotionalSchematic.tsx:15`** — Appears in a JSDoc comment describing schematic routing logic (`* complexity is the pain point`). This is developer-facing technical documentation, not customer copy. Left unchanged.

2. **Banned-word sweep beyond "unlock"** — The voice rules list several banned words (leverage, seamless, holistic, robust, crucial, delve, best practice, end-to-end). A full sweep returned zero hits in customer-facing copy outside of `app/clients/`. No action needed.

3. **`app/clients/`** — Entirely excluded per hard rules. Contains signed commercial documents and auth-gated portal code.

4. **Pricing figures** — No changes to any pricing anywhere.
